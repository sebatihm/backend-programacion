services:
  database:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test
    volumes:
      - db-data-proyect:/var/lib/mysql
    ports:
     - 3306:3306
volumes:
  db-data-proyect: